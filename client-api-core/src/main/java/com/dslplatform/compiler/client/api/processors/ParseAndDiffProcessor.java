package com.dslplatform.compiler.client.api.processors;

import com.dslplatform.compiler.client.io.Logger;

public class ParseAndDiffProcessor extends DiffProcessor {
    public ParseAndDiffProcessor(
            final Logger logger) {
        super(logger);
    }
}
